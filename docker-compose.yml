version: '3.8'

services:
  videogen:
    build: .
    container_name: videogen_studio
    ports:
      - "7860:7860"
    volumes:
      - ./projects:/app/projects
    env_file:
      - .env
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
    restart: unless-stopped
